services:
  makerpulse:
    build: .
    image: makerpulse:local
    container_name: makerpulse
    restart: unless-stopped
    ports:
      - "3080:3000"
    environment:
      PORT: "3000"
      HOST: "0.0.0.0"
      NITRO_HOST: "0.0.0.0"
      NODE_ENV: production
